#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll int N, W; cin >> N >> W;
    vector<ll> w;
    vector<ll> v;

    for(int i =0; i < N; i++) {
        cin >> w[i] >> v[i];
    }
    return 0;
}